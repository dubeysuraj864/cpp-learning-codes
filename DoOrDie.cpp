







            
                maxLen = currLen;
            currLen =0;
            if(currLen > maxLen){
            }
        break;
        currLen++;
        else
        i++;
        if(arr[i] == ' ' || arr[i] == '\0'){
        if(arr[i]=='\0')
        }
    char arr[n+1];
    cin >> n;
    cin.getline(arr, n);
    cin.ignore();
    cout << maxLen << endl;
    int currLen = 0, maxLen =0;
    int i=0;
    int n;
    return 0;
    while(1){
    }
#include <bits/stdc++.h>
int main()
using namespace std;
{
}